# --- Telegram bot ---
aiogram==3.30.0

# --- Web / health ---
fastapi==0.141.1
uvicorn[standard]==0.52.4

# --- Google Gemini ---
google-genai==2.19.0

# --- Database ---
sqlalchemy[asyncio]==2.0.52
alembic==1.19.1
asyncpg==0.31.0
# psycopg2-binary is used only by Alembic's synchronous migration driver.
# Prebuilt wheels track Python releases closely - if `pip install` tries to
# build it from source (no matching wheel yet for a brand-new Python
# version), either run migrations from a Python 3.12 environment/image, or
# install PostgreSQL's libpq/pg_config so it can build locally.
psycopg2-binary==2.9.10

# --- Background tasks ---
celery[redis]==5.6.3
redis==6.4.0

# --- Document processing ---
pymupdf==1.28.2
Pillow==12.3.0
python-docx==1.2.0
openpyxl==3.1.5

# --- Validation / config ---
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3

# --- Resilience ---
tenacity==9.1.4

# --- Testing ---
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-mock==3.15.1

# --- Lint / type-check ---
ruff==0.16.4
mypy==2.3.1
